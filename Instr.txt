I would like to create a new language. So basically, this language has the following functionalities:\n1. Hot and cold variables:\nUse the hot keyword before a func (for defining a function or variable name) to indicate that this should be used as inline: Eg: hot x (int) = 5; out(x); when compiling this turns to out(5);\nCold is optional: cold x (int) = 5 is the same as x (int) = 5;\nNOTE THAT hot x must be compile-time constant (Tho i hate it)\nA hot variable cannot be reassigned after declaration.\n2. variable types, when defining, should be in the format of var_name (type) = value; Note that this is only an optional option, similar to how u can do x:int=5 in python.\n3. when defining a class, use the keyword family familyname {}. Class inheritence can be done be family familyname(inheritence_from_classname){}. The end of a family does NOT need a semicolon. This is also the same when defining a function using func functionname(params){}. Please note that the params are input as these: typename varname; Or if a variable needs to be in various types, you can use [type1, type2] varname. For example, (assume pandas lives in here: [pandas.Series, pandas.DataFrame] data. We allow cases where, if a function does not need any input params, to ignore writing the parenthesis. Eg: func printHelloWorld{}. Single line functions are defined as: func add(int a; int b;) => a + b; Note one-line functions do end with semicolons because they are expression-style.\nNOTE: To return, use the ret keyword. EG: ret 5;\nNote: Initializing a family is defined by func init(){}; Notice this is init() not __init__();\nCalling self (or this) is names as: me.(whatever). So if you code like self.x= 5; Now use me.x=5;\n4. All other lines need to end with semicolons.\n5. To start a comment, use // or /* */ for multiline commenting. Commenting in the middle with the /* */ is permitted eg: hot x /* make it HOT */ (int) = 5;\n6. The start of a function is func m{}. This should not contain any inputs.\nNOTE: If user prefers, we CAN ALLOW semicolons at the end of families and functions. We can also allow func m(){} (which has no params). We suggest you dont do it but if ur trans from c++ or python feel free to do so.\nNOTE: ret 0; is not mandatory at the end.\n7. To print a line to console, use out(). Note that this auto puts a \n to the end. So if you want to not have this, use slout() (Which is abbreviated for single-line output)\n8. To read in all inputs (And put them into a multi-line string), use in(). If you only want to read PER LINE, use inln()\n9. To import other modules, use the takepkg keyword. It should be followed by the filename of the module. You only have to import ONCE per module. But be sure that this file has to contain all the stuff. Either it is a bundle (like inside this main file it takepkgs all other supporting files) or it's only one single file that you put all your functions in.\nTo use the import...as... statement, u can use takepkg package_name_which_is_long = pkg;\nRemember to add semicolons no matter if u use the = or not!\n10. Conditionals\ni) The if loop: if (stuff) {}\nii) support for elif (){}\niii) The repeat x{} where x is the time you want to repeat. EG: repeat 5 {} means repeat this stuff for 5 times\niv) match cases: define a match variable {} Inside the curly braces, do: for example:\nmatch x{ // First match->exit;\n1 => /* here you can just do a one liner code, or use {} if you want multi-line of code altho this is not recommended*/ Remember that for these cases, you have to end with an ;, even if its {}. EG: {code};\nthe default is __ => {Do sth} (Note that this is double-underscore, not single underscore).\nNote cases can use a range, ranges are defined as range(a, b) which is a range of [a,b)\nv) For loop: A for loop is defined as for (variable, condition_of_variable, variable_change){}. This is very similar to that in c++ except the semicolons are replaced with commas. For multiple conditions, for example, put them in parenthesis. EG: for ((i=1 also n=0), (i>5 or n<-2), (i++ also n--)) Note that here, when you are defining these, you MUST use the keywords also, or, and/or not. Commas inside the parenthesis (eg: (i=1, n=0)) Are NOT allowed.\n11. Error handling:\nThe try...except...finally block is now: run{...} handle (errors){} now_do{} (now_do is optional)\nAllow handle(Error1, Error2) rather than handle([Error1, Error2])\n(Note: Need to specify some error types. The base class error (similar to Exceptions in python) is Error)\nraising an error is now err(errortype, message)\n12. Data structures\ni) Arrays. Similar to lists in python and arrays in C++. The difference is that the length is not fixed and it supports different types. EXCEPTION: When user defines an array eg: arr (array[type]) = a(...), then the array MUST ONLY consist stuff OF that type. This is called an type restriction. An typical array is like: a(1,2,3,4,5);\nii) Sets. Similar to a set() in python. No dups allowed. defining: s (set) = s(...); Please note a set is defined to be s(...); Similar type restrictions can be made for sets. Eg s (set[int]) = s(1,2,3,4,5)\niii) Pairs. Similar to a tuple in python, but it is mutable. It is defined as p (pair[type1, type2]) = p(stuff1, stuff2);\nNotice it is defined as p(...);\nNote pairs are the only type that, with type restrictions, still allows two types.\niv) Modifying these structures\na. For arrays, use array.add() to add element to the END of array. Use array.iget(idx) to get the value at index idx. Use array.pop() to remove the first element. Use array.lpop() to remove the LAST ELEMENT. Use array.iremove(idx) to remove value at index idx. Use array.get(val) to get the index of the FIRST OCCURANCE of val. Use array.rget(val) to get the index of the first occurance of val BUT IN REVERSE ORDER. Use array.remove(val) to remove the value (FIRST OCCURANCE). To modify an element, use array.modify(idx, val) to change the value at index idx to val. Use array.change(prev, curr) to change ALL values in the array which is prev to the value curr. Use array.sort() to sort an array from largest to smallest. Use array.asort() (For ascending sort for the opposite). Use array.rev() or array.reverse() To reverse the whole array.\nb. It is similar for sets. NOTE that we do not encourage users to do set slicing, and if they REALLY need to do it, please sort it first.\nc. For pairs, use pair.first, pair.second to get the first and second values. To change values, simply do: pair.first = newval;\nd. To convert stuff into these, just do a(...), s(...), or p(...). NOTE THEY R BUILT-IN constructors\ne. Arrays and Sets support slicing, just do array.slice(startidx, endidx) This slices the array of [startidx, endidx). If you want to slice by start idx and length, do array.lenslice(startidx, len)\n13. Basic types:\nint (-2^31~2^31-1)\nlongint (-2^63~2^63-1)\nstring\nchar (THIS IS SPECIAL)\nfloat\nNote that char is special because it cannot be defined by single quotes. Both single quotes and double quotes lead to strings. To convert one single character eg: 1 or "1" to char, use the CHAR_CNVRT() function; Note this is all caps.\nNote multi-line strings are allowed to be defined as """ lines """\nTo split a multi-line string into an array, use multilinestr.split(). Note this creates an array[string] type. You cannot add values of other type to it.\nFor type conversions, do int(), longint(), string(), float().\n14. To define a constant, use the fixed keyword. fixed should come before hot and cold.\n15. Boolean:\ntrue\nfalse (Note: If user inputs fasle, we'll also count them as false, because i always do this typo when i type fase)\nWE DO NOT ACCEPT ANY OTHER TYPOS. FASLE IS ACCEPTED BECAUSE THIS IS MY PAIN. thx.\nnull\n16. Special:\nSometimes u may want to define an array of errors. This is allowed but not allowed when using then handle(){}. Here, you can use &~array to unpack it, or use the function UNPACK_ARR();\n// Add more if needed\n17. Operations:\n+ - * / %\n**\n== !=\n< <= > >=\n=\n+= -=\n++ --\nNOTE: We currently have no intention of introducing the >> and <<.\nSPECIAL. Illegal opers:\ni) WRONG ORDER OF fixed. Fixed must be before cold. (fixed x = 5 means same as fixed cold x=5;)\n\nFINAL: QUICK EXAMPLE:\ntakepkg math_tools = mt;\n\nfixed hot pi (float) = 3.14159;\n\nfunc area(float r;) => pi * r * r;\n\nfamily Circle{\n    func init(float r;){\n        me.r = r;\n    }\n\n    func size(){\n        ret area(me.r);\n    }\n}\n\nfunc m{\n    nums (array[int]) = a(3,1,4,1,5);\n    nums.asort();\n    out(nums.iget(0));\n\n    c = Circle(5);\n    out(c.size());\n}\n\nI intend this function to be called marslang, with the suffix being .mrs\nThis is the v0.1 of my language.

I want you to implement this language in Unknownuserfrommars/marslang, under the branch "1". This should include a parser, lexer, AST, and so on, all the stuff u need to make this work. In the end it should have an compiler so that it can take in a file and compile it to {whatever language or executable}
EG: compiler hello.mrs should compile to something.
Now i dont care WHAT language you are converting this to, or if you convert it to binary or stuff, just implement it. Take your time. If you have any questions about the language, ask me.
Please start your work. Take your time we're in no rush




Ok, Now after sharing advice with my friends, there are some thing i want to fix> I think that's totally ok since we're in the alpha stage so, yea
But here r some things to change and i want u to do a v0.2 push, ok?
1. change the now_do{} (which acts like the finally block) to then{}
2. change the a(), s(), p() creating method to arr(...), set(...), pair(...) (Keep the type name as array[type], set[type] and pair[type,type])
3. Also change the for loop logic to for(iterator, condition, change_iterator), which is basically a c++ for loop but u change the ";" to ","
4. Also can u change the compiling stuff to a mini-VM like stuff for Python? Like at least for now?
That's all for v0.2. Work and push, good luck man~ love you.



When i try to run the test, it will not work. For 0.3 version, Please UPDATE the compiler and CLI.py to make sure it is compatible with the 0.2 changes. Also, please add a -v or --verbose tag to the compiler so that it'll output a verbose compiling output.
Also please make sure you write good readme. You can also do a docs directory where u store all of the documentation in .md maybe. This needs to be a really detailed doc including quickstart and api docs whatever.


- Parser fragility: parse_type_until() is unused and has a bug (tok referenced before assignment) in marslang/parser.py:160, suggesting incomplete refactors and hidden crashpaths 
- Feature drift: cold is parsed but ignored (marslang/parser.py:99-114), and Index exists in AST but isn't parsed or executed (marslang/ast.py:180-184). This is dead code or missingsyntax/runtime- Keyword typo baked in: fasle is a keyword in marslang/lexer. py:26 and parsed as booleanin marslang/parser.py:436-438. Likely a typo for false, leads to confusing user behavior and doc mismatch. 
- Unsafe lookahead: parse _decl_or_stmt () directly indexes self. tokens[ self.pos + 1] without bounds checks (marslang/parser.py:138), which can throw IndexError at EOF.- Runtime semantics are partial: type restrictions only apply to array/set elements (marslang/runtime.py:529-539), union types are parsed but never enforced; and/or is accepted but evaluates only as and (marslang/runtime.py:516-521).- Error handling UX: CLI wraps compile errors but not runtime errors when --run is used (marslang/cli.py:54-56), so users get raw Python stack traces.Tests are extremely thin: only a few integration tests in tests/test_compiler.py cover happy paths. Lexer, parser error handling, type enforcement, and edge cases are effectively untested.

Can u do a v0.4 for these security fixes?
Also REMEMBER UPD THE DOCS & README


Oh yea for the /bin/compiler/ can u make 2 different versions based on unix or windows operation systems? Make it version 0.4.1 ty